# 并发Debug

>`info threads #sreach current thread`
>set scheduler-locking on # lock threads on only one 

## switch one thread

>treahs thid


# Peterson

![[Pasted image 20260615110304.png]]
`T_producer` : 生产者线程
`mutex_lock` : 获取锁
`while` :  检查是否处于临界区域

`cond_wait` : 释放锁

